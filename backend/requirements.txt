# requirements.txt
# Lista de librerías Python que necesita el proyecto.
# Se instalan automáticamente cuando Docker construye el contenedor.

# --- Framework principal ---
Django==5.1.4

# --- Base de datos ---
psycopg2-binary==2.9.10     # conector entre Python y PostgreSQL

# --- Variables de entorno ---
python-decouple==3.8        # para leer variables del archivo .env de forma limpia

# --- Admin mejorado ---
django-unfold==0.42.0       # interfaz de administración más moderna

# --- Frontend HTMX ---
django-htmx==1.21.0         # integración de HTMX con Django

# --- Reportes y exportación ---
openpyxl==3.1.5             # generación de archivos Excel
reportlab==4.2.5            # generación de archivos PDF (facturas)